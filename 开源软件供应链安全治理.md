# 开源软件供应链安全治理

# 一、背景

随着互联网服务规模不断扩大，现代软件系统已经很少完全由单一组织从零开发完成。一个网站、App或企业平台，往往依赖大量开源组件、第三方SDK、云服务接口、容器镜像和自动化构建工具。软件开发效率因此得到提升，但系统安全也越来越依赖外部组件和第三方服务的可信程度。

在这种模式下，攻击者不一定直接攻击目标系统本身，而可能通过开源组件漏洞、恶意依赖包、软件更新污染、构建流水线篡改或供应商被入侵等方式进入目标系统。由于这些环节通常处在软件生产和交付链条中，风险一旦发生，可能沿着供应链影响大量下游用户和组织。

因此，软件供应链安全治理关注的不只是某个具体漏洞如何修复，而是如何在软件开发、构建、交付、部署和更新维护的全过程中，管理组件、流程、工具和供应商，使软件来源可见、过程可信、责任可追、风险可控。本文将从基本概念、风险来源、典型案例和治理路径四个方面展开分析。

---

# 二、软件供应链安全的基本概念

所谓软件供应链，可以理解为软件从产生到使用所经历的完整链条。它不仅包括开发人员编写的源代码，也包括软件开发过程中使用的开源库、第三方组件、构建工具、测试环境、发布平台、部署系统、运维工具以及后续更新机制。

如果把软件比作食品，那么软件供应链就类似于食品生产链。食品安全不仅要关注最终摆在货架上的成品，还要关注原材料来源、加工过程、运输过程、包装过程和销售过程。软件也是一样。软件安全不仅要看最终系统有没有漏洞，也要看软件的“原材料”是否可信，生产过程是否安全，交付渠道是否可靠，后续更新是否可控。

从这个角度看，软件供应链安全关注的问题主要包括：

第一，软件用了哪些开源组件和第三方依赖；

第二，这些组件是否存在已知漏洞、恶意代码或维护风险；

第三，软件构建和发布过程是否可能被篡改；

第四，软件供应商、外包团队和第三方SDK是否可信；

第五，软件出现安全事件后能否快速定位、修复和追责。

因此，软件供应链安全治理并不只是一个纯技术问题，而是一个典型的互联网治理和安全管理问题。它涉及开发团队、安全团队、采购部门、运维部门、法务合规部门、管理层以及外部供应商之间的协同。

这也正是它与漏洞管理、平台内容安全的区别所在。漏洞管理更关注某个系统或组件中已经发现的安全缺陷如何修复；平台内容安全更关注用户发布的内容是否违法违规；而软件供应链安全治理关注的是整个软件生产和交付链条中的系统性风险。

---

# 三、开源软件供应链安全的重要性

软件供应链安全之所以重要，并不是因为出现了某一种新的单点漏洞，而是因为现代软件的开发方式、攻击路径和治理目标都发生了变化。软件安全已经不再只取决于企业自己编写的代码，也取决于外部组件、开源生态、构建流程和软件分发渠道是否可信。

## 软件开发方式发生变化：从“自研”变成“组装”

现代软件很少完全由企业从零开发完成，而是大量复用开源组件、第三方库、包管理器、CI/CD 工具、容器镜像和云服务。开发者可以通过 npm、PyPI、RubyGems、Maven 等包管理器快速引入第三方代码，从而提高开发效率、降低开发成本。

但是，这种“组装式开发”也改变了软件安全边界。系统安全不再只取决于自身业务代码，还取决于所使用组件的安全性、维护状态和分发渠道。NDSS 2021 的论文 *Towards Measuring Supply Chain Attacks on Package Managers for Interpreted Languages* 研究了 npm、PyPI、RubyGems 等解释型语言包管理器中的供应链攻击，指出包管理器已经成为现代软件开发的重要基础设施，同时也可能被攻击者用于分发恶意软件包。该研究发现了数百个恶意包，说明开源包管理生态已经成为供应链攻击的重要入口。

因此，当软件开发从“自研”转向“组装”后，企业不仅要关注自己写的代码是否安全，还要关注依赖包从哪里来、是否仍在维护、是否存在漏洞，以及间接依赖是否可控。

---

## 攻击者目标发生变化：从攻击单个系统变成攻击上游生态

传统网络攻击往往直接针对目标系统本身，例如利用 Web 漏洞、弱口令、错误配置或数据库漏洞进入系统。但在软件供应链场景中，攻击者不一定直接攻击最终用户系统，而是可以选择攻击更上游的位置，例如开源项目维护者账号、代码仓库、包管理器、构建流水线、制品仓库或发布流程。

这种攻击方式更加隐蔽，也更容易扩大影响范围。一旦攻击者成功污染上游组件，恶意代码就可能随着正常的软件依赖、更新机制或构建流程传播到大量下游项目中。开发者信任包管理器，企业信任开源组件，用户信任软件更新，攻击者正是利用了这些信任关系。

S\&P 2023 的论文 *SoK: Taxonomy of Attacks on Open\-Source Software Supply Chains* 对开源软件供应链攻击进行了系统分类。论文将攻击组织成覆盖代码贡献、项目维护、构建过程和软件包分发等多个阶段的攻击树，并整理了 107 类攻击向量、94 个真实攻击事件和 33 类缓解措施。这说明软件供应链攻击不是某一种单一漏洞，而是贯穿软件开发、协作、构建、发布和分发全过程的系统性攻击面。

---

## 治理目标发生变化：从“发现漏洞”变成“建立可信链条”

传统软件安全治理主要关注漏洞发现和修复，例如代码审计、漏洞扫描、渗透测试和补丁管理。这些措施仍然重要，但对于软件供应链安全来说，仅仅发现漏洞是不够的。供应链攻击可能表现为依赖包被污染、构建环境被篡改、发布包被替换、维护者账号被盗用，或者最终软件制品无法证明来源。

因此，软件供应链治理需要回答更基础的问题：这个组件是谁开发和维护的？代码是否经过审查？依赖是否来自可信来源？构建环境是否可控？最终制品是否真的由对应源码构建而来？软件包发布后是否被篡改？当某个组件出现漏洞时，能否快速判断哪些系统受到影响？

这也意味着治理目标要从“发现漏洞”扩展为“建立可信链条”。SBOM 用于说明软件由哪些组件组成，SCA 用于识别依赖漏洞，制品签名和哈希校验用于验证发布包是否被篡改，SLSA 则进一步关注软件是否由可信源码、可信构建环境和可信流程生成。NIST SSDF 也从安全开发实践角度强调，应将安全要求嵌入软件开发生命周期。

---

因此，软件供应链安全的重要性在于：它把网络安全治理对象从单个系统扩展到了软件生态，把攻击防护对象从运行环境扩展到了开发和交付过程，也把治理目标从漏洞修复扩展到了来源可信、过程可信、制品可信和责任可追。

---

# 四、开源软件供应链各环节的风险来源

开源软件供应链涉及多个参与主体和系统组件。软件从代码产生到最终被下游用户安装和运行，通常要经过代码贡献、审查合并、源码托管、自动化构建、依赖拉取、软件打包、制品分发以及用户部署等多个阶段。在这一过程中，贡献者、项目维护者、版本控制系统、构建系统、分发平台、系统管理员和下游用户都可能参与其中。

从安全角度看，供应链中的每一个节点都可能成为攻击入口。攻击者既可以在源头污染代码，也可以利用维护者权限、依赖解析机制、构建流水线或软件分发平台进行攻击。因此，分析开源软件供应链风险时，需要按照不同模块分别理解其风险来源。

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=NGNlODljOTVhYjVhNTNjNTZjMmYyMzkwMDFlNzcyYjNfNzljY2M1NjNjZTEwMjlkYjZjMjQ4NWY5MmVmNmU0NWRfSUQ6NzY0NzQyOTQ1OTEwMTE0MjI0MF8xNzgwNzUwODQwOjE3ODA4MzcyNDBfVjM)

## 贡献者模块：代码入口风险

贡献者是开源软件供应链中最前端的参与者，通常通过提交 Pull Request 或 Merge Request 的方式向项目贡献代码、配置文件、依赖声明或构建脚本。

这一环节的主要风险在于：**不可信内容可能从****正常****代码入口进入项目。**

攻击者可能伪装成普通贡献者，提交包含恶意逻辑的代码。这类代码不一定表现为明显后门，也可能隐藏在功能修复、性能优化、代码重构或边界条件处理中。如果维护者审查不足，恶意逻辑就可能被合并到主分支。

更隐蔽的情况是，攻击者不直接修改业务代码，而是修改依赖文件、构建配置或自动化脚本。例如 package\.json、requirements\.txt、setup\.py、Dockerfile、CI/CD workflow 等文件会影响依赖安装、构建执行和软件发布过程。由于它们看起来不像核心业务代码，往往更容易被忽视。

贡献者还可能通过新增第三方依赖扩大攻击面。一个新增依赖本身可能存在漏洞、维护状态较差，甚至可能是攻击者控制的恶意包；同时，它还可能继续引入更多传递依赖，使风险沿依赖树扩散。此外，大规模格式化、自动生成文件、压缩代码或二进制文件提交也会增加审查难度，使真实改动不易被发现。

## 项目维护者模块：审查与权限风险

项目维护者在开源软件供应链中处于核心位置。他们负责审查贡献者提交的代码，决定是否合并变更，并通常拥有较高的仓库管理和软件发布权限。

这一环节的主要风险在于：**维护者的判断****失误****、权限****过度集中以及账号安全失守。**

维护者账号一旦被接管，攻击者就可能直接合并恶意代码、修改构建配置、触发恶意构建任务，甚至发布被污染的软件版本。账号接管的方式包括泄露凭据复用、钓鱼攻击、社会工程、会话劫持、弱密码以及本地环境被攻破等。

维护者也可能受到长期社会工程影响。开源社区通常依靠长期贡献、社区声誉和协作信任维持项目运行。攻击者可以通过长期提交低风险贡献建立可信形象，逐步获得维护权限；也可能通过欺骗、施压、贿赂或勒索等方式影响维护者行为。这类风险不完全属于技术漏洞，但会直接改变项目安全状态。

此外，维护者权限集中会放大风险。如果单个维护者同时拥有代码合并、构建配置修改、制品发布和项目管理权限，一旦该账号失陷，攻击者就可能绕过多个环节的正常控制，直接影响最终软件制品。因此，维护者模块的风险不仅是账号安全问题，也是权限治理问题。

## 版本控制系统模块：源码托管风险

版本控制系统是开源项目存储和管理源码的核心平台，常见形式包括 GitHub、GitLab、Gitee 或企业内部 Git 平台。它保存项目源码、提交历史、分支、标签、Issue、Release 以及协作记录，是后续构建与发布流程的基础。

这一环节的主要风险在于：**源码仓库一旦被污染，后续构建和发布都会建立在不可信基础之上。**

攻击者可以通过篡改合法项目源码注入恶意代码。一旦恶意代码进入源码，它就可能影响所有下游用户，无论这些用户是直接克隆源码，还是安装由该源码构建出的预编译包。

攻击者可能绕过正常贡献流程直接篡改 VCS。正常情况下，贡献者提交的代码需要经过维护者审查，但如果攻击者能够控制维护者账号、管理员账号，或者利用 VCS 配置错误和系统漏洞，就可能绕过 Pull Request / Merge Request 审查流程，直接修改代码库。这种攻击方式比普通恶意贡献更危险，因为它可以避开社区协作中的部分可见审查机制。

除了源码本身，VCS 中的构建配置和元数据也具有供应链安全意义。构建脚本、依赖声明、CI 配置、发布配置和版本标签都会影响后续构建系统如何获取依赖、执行任务和生成制品。如果这些内容被篡改，即使业务代码本身没有变化，最终产物也可能受到影响。

VCS 还容易成为敏感信息泄露点。开源项目或企业项目中，误提交 API Token、私钥、发布凭证、云服务密钥和数据库密码并不少见。一旦这些信息进入仓库，即使后续删除，也可能已经被攻击者捕获，并进一步用于攻击构建系统、制品仓库、云平台或生产环境。

## 依赖拉取模块：依赖解析与包源风险

在构建过程中，系统通常需要从包仓库、镜像仓库、系统源或其他分发平台拉取依赖。

这一环节的主要风险在于：**项目最终制品不仅由自身源码决定，****还****由大量直接和传递依赖共同决定。**

攻击者可以创建全新的恶意包，并通过宣传、伪装或诱导方式吸引开发者使用。这类攻击并不需要篡改已有项目，而是通过构造看似有用的新包进入下游依赖树。一旦被项目引入，恶意包就可能在安装、构建或运行阶段执行攻击逻辑。

名称混淆是依赖拉取过程中的常见风险，包括 typosquatting、combosquatting、改变词序、改变分隔符、冒充内置包、品牌劫持和相似名称攻击等。这类攻击利用开发者或自动化系统对包名的误认，使下游项目错误安装攻击者控制的恶意包。

攻击者还可能滥用依赖解析机制。包管理器通常会根据版本范围、仓库优先级、命名空间、配置文件和解析规则决定下载哪个包。攻击者可以利用这些机制中的弱点，使构建系统从错误来源下载恶意包，或者通过依赖混淆方式让公共仓库中的恶意包覆盖内部包名。

传递依赖进一步放大了这一风险。下游项目可能只显式依赖少量组件，但包管理器会自动解析并下载大量间接依赖。下游用户往往很难逐一审查这些依赖，因此在实践中不得不高度依赖上游生态的可信度。

## 构建系统模块：构建过程污染风险

构建系统负责从 VCS 获取源码，拉取依赖，并执行编译、测试、打包和发布准备操作。

这一环节的主要风险在于：**即使源码本身可信，构建过程仍然可能被攻击者污染，从而导致最终制品不可信。**

攻击者不一定需要修改源码，只要能够影响构建任务、构建环境、构建脚本或构建依赖，就可能改变最终生成的软件制品。例如，恶意构建脚本可以在编译过程中下载额外载荷，受污染的构建环境可能使每次构建都生成带后门的制品。

VCS 本身也是高价值攻击目标。CI/CD 平台、Jenkins 服务、GitHub Actions、GitLab CI 或企业自建构建系统，如果存在弱配置、漏洞、权限控制不足或管理员账号失陷，攻击者可能直接篡改构建任务、读取构建日志，或获取构建过程中使用的凭证。

VCS 通常还持有大量敏感凭证，例如源码仓库访问 Token、依赖仓库访问凭证、制品仓库发布 Token 和云服务密钥。恶意构建任务一旦能够读取环境变量或 secret，就可能造成凭证泄露，并进一步影响发布、部署和运行环节。因此，构建系统既是软件生产线，也是供应链攻击中的关键目标。

## 分发平台模块：软件包传播风险

分发平台负责托管和分发预构建软件制品，例如 npm、PyPI、Maven Central、Docker Hub、GitHub Releases、CDN、镜像站、代理仓库或企业内部制品库。

这一环节的主要风险在于：**分发平台是软件包传播的关键节点，一旦被污染，风险会迅速扩散到下游用户。**

攻击者可以发布合法包的恶意版本。实现方式可能包括接管包维护者账号、成为项目维护者、攻破维护者系统，或者利用发布流程中的权限缺陷。一旦恶意版本发布到公共仓库，下游用户可能会通过正常更新机制自动安装。

分发平台或托管系统本身可能被注入恶意内容。如果攻击者攻破包仓库、镜像站、CDN、代理仓库或其管理员账号，就可能替换软件包、修改元数据，使用户下载到被篡改的产品。

分发过程还可能受到网络和解析层面的攻击。例如中间人攻击、DNS 缓存投毒、篡改合法 URL、滥用下载连接和解析机制，都可能导致用户或构建系统下载到攻击者控制的软件包。

此外，悬空引用也是分发环节的重要风险。某些项目、域名、包名、URL 或资源标识符被废弃后，攻击者可能重新注册或接管这些资源，使旧引用指向恶意内容。由于下游项目可能仍然依赖这些旧引用，风险会在不知情的情况下继续存在。

## 下游用户模块：风险继承与可见性不足

下游用户是开源软件供应链的最终消费者，可以直接从 VCS 克隆源码并构建，也可以从分发平台安装预构建软件包。

这一环节的主要风险在于：**下游用户通常继承上游所有环节的风险，却****很难获得对上游过程的完整可见性。**

下游用户可能安装被污染的预构建软件包。如果软件包在发布前的构建阶段被污染，或者在分发平台中被替换，下游用户通常很难仅凭包名、版本号或项目声誉判断其是否可信。

即使用户选择从源码构建，也并不意味着完全安全。构建脚本仍然可能访问外部包仓库、下载依赖、执行安装脚本，并受到依赖解析机制影响。换言之，源码构建减少了对预构建制品的依赖，但并没有消除供应链风险。

下游用户还面临依赖规模过大的问题。一个项目可能包含大量直接依赖和传递依赖，用户很难对每一个组件进行完整审查。由于对开源项目安全实践缺乏控制，下游用户在很大程度上只能依赖社区发现漏洞、维护者及时修复以及分发平台进行基本治理。

## 系统管理员模块：基础设施与高权限风险

系统管理员负责配置、维护和运行 VCS、构建系统、分发平台等关键基础设施。

这一环节的主要风险在于：**管理员权限通常是供应链基础设施中的高价值权限，一旦失控，可能影响整个供应链。**

管理员账号被接管后，攻击者可能修改访问控制策略、篡改仓库内容、读取密钥、修改构建任务、替换软件包，甚至删除审计记录。与普通贡献者或普通开发者相比，管理员权限更接近供应链基础设施的控制层，因此其失陷影响更大。

系统配置错误会引入供应链级风险。例如，VCS 权限过宽、构建系统暴露在公网、CI 任务默认拥有过高权限、外部贡献者可以访问敏感变量、制品仓库允许覆盖已有版本、分发平台缺少强身份认证等，都会扩大攻击面，降低攻击者实施供应链攻击的门槛。

此外，基础设施本身可能存在漏洞。VCS 服务、CI/CD 平台、包仓库系统、CDN、代理仓库或插件如果存在安全漏洞，攻击者可以绕过正常账号权限，直接影响源码、构建任务或分发制品。

---

# 五、开源软件供应链安全治理路径

软件供应链安全治理不能只停留在“发现漏洞后及时修复”的层面。它更强调对软件生产和交付过程的全过程控制。因为供应链风险往往不是在系统上线后才突然出现的，而是可能在软件采购、外包开发、开源组件引入、代码合并、自动化构建、软件发布和后续更新等环节中逐渐形成。

因此，软件供应链安全治理的重点，不是单纯增加一次安全检测，而是要把安全要求嵌入软件生命周期的关键节点中。可以理解为：在组件进入项目前进行准入审查，在代码合并前进行安全检查，在构建过程中保证环境可信，在发布前验证软件完整性，在上线后持续监测风险，在漏洞出现后快速定位影响范围并完成修复。

## 开源组件准入治理

开源组件准入治理是软件供应链治理的前置环节，解决的是**外部开源组件能否被引入项目**的问题。现代软件大量复用第三方组件，一个组件进入项目，实际上也引入了它背后的维护者、依赖树、漏洞历史、许可证、构建方式和发布机制。因此，这一阶段的核心不是简单判断组件“能不能实现功能”，而是评估它是否可信、是否安全、是否合规、是否适合作为当前系统的依赖。只有在组件进入项目之前建立准入门槛，后续的依赖管理、漏洞治理和构建发布安全才有基础。

首先，需要评估项目的基础可信度。一个适合引入的开源项目应当具有明确的官方仓库、稳定的项目主页、清晰的功能说明、安装文档、贡献说明和许可证声明。如果一个项目来源不清晰、许可证不明确、文档缺失，或者无法确认其官方发布渠道，那么即使它能满足功能需求，也不应轻易引入。在这方面，可以参考 OpenSSF Best Practices Badge。该项目通过 passing、silver、gold 等不同级别，展示开源项目是否遵循一组安全导向的开发最佳实践。对于企业来说，这类最佳实践徽章可以作为组件准入的基础参考，用来判断项目是否具备基本的工程规范和安全意识。

其次，需要评估项目的维护健康度。开源组件的风险不仅来自已有漏洞，也来自“无人维护”。如果项目长期没有提交、issue 无人响应、PR 长期堆积、release 停滞，或者只有极少数维护者，那么一旦出现高危漏洞或供应链攻击，下游用户可能无法及时获得修复。在维护健康度评估上，可以使用 OpenSSF Scorecard。Scorecard 会通过自动化检查评估开源项目在源码、构建、依赖、测试和维护等方面的安全风险，并给出相应评分。它可以帮助安全团队快速识别项目是否存在维护不足、缺少分支保护、缺少 CI 测试、依赖管理不规范等问题。

再次，需要评估项目的安全实践成熟度。准入评估应关注项目是否提供 SECURITY\.md，是否有漏洞披露渠道，是否说明安全支持版本，是否启用分支保护，是否要求代码审查，是否使用 CI 测试，是否对 release 或制品签名。对于安全关键组件，例如加密库、认证库、网络协议解析库、反序列化组件和运行时框架，还应提高准入标准。

对于关键组件，准入评估还应关注构建和发布可信度。下游用户最终使用的往往不是源码，而是构建后的软件包、镜像或二进制文件。因此，应检查项目是否提供哈希校验、制品签名、构建来源证明，是否采用 SLSA 等供应链安全框架，是否能够证明制品来自对应源码和可信构建流程。SLSA 可以作为评估制品可信度的重要参考。它提供软件供应链安全规范，帮助组织评估所消费制品的可信度，并提供提升软件安全的可操作清单。

此外，还需要评估依赖和传递依赖风险。一个组件本身可能没有明显问题，但它依赖的上游组件可能存在高危漏洞、恶意包、废弃包或许可证冲突。因此，准入阶段应对候选组件生成 SBOM，结合 SCA 工具扫描其直接依赖和传递依赖，识别漏洞、许可证和维护风险。对于依赖链过深、依赖数量异常庞大或包含高风险安装脚本的组件，应提高审查级别。

最后，还需要评估合规风险。开源组件准入不仅是安全问题，也是法律和合规问题。应检查组件许可证是否明确，是否与企业产品形态兼容，是否存在 copyleft 义务、商业使用限制、专利条款风险或多许可证冲突。对于许可证不清晰或来源不明的组件，应避免进入生产系统。

从流程上看，开源组件准入可以分为五步。第一步，由开发人员提交组件引入申请，说明组件名称、版本、用途、引入原因、使用场景和是否处于关键路径。第二步，使用自动化工具进行初筛，包括 OpenSSF Scorecard、OpenSSF Best Practices Badge、SCA、SBOM、许可证扫描和恶意包检测。第三步，结合自动化结果和业务场景进行人工风险分级，将组件划分为低风险、中风险、高风险和禁止引入。第四步，给出准入决策，包括允许引入、限制引入、整改后引入、替换组件或禁止引入。第五步，形成准入记录，将组件名称、版本、许可证、风险等级、依赖关系和使用场景纳入后续依赖治理范围。也就是说，准入治理解决的是组件进入项目之前的筛选问题，而组件进入项目之后，还需要通过持续的依赖可视化和漏洞影响分析来判断其长期风险。



## 依赖可视化与漏洞影响治理

开源组件准入治理解决的是组件引入前的风险筛选问题，但组件一旦进入项目，其风险状态并不是静态不变的。组件版本可能更新，传递依赖可能变化，上游项目可能停止维护，新的漏洞也可能在软件上线后才被披露。因此，依赖可视化与漏洞影响治理关注的是组件引入之后的持续管理问题，即组织是否能够持续知道系统中使用了哪些直接依赖和传递依赖，哪些组件存在已知漏洞，以及这些漏洞是否真正影响当前项目和运行环境。

这一环节的基础手段是 SBOM 和 SCA。SBOM 用于记录软件制品中包含的组件、版本、许可证和依赖关系，使组织能够获得“用了什么”的可见性；SCA 则将组件版本与漏洞库、安全公告和许可证规则进行关联，发现存在已知风险的依赖。然而，仅有 SBOM 和 SCA 仍然不够，因为传统组件级扫描往往只能说明“某个依赖版本存在漏洞”，却不能准确判断该漏洞是否在当前项目中可达、是否会被真实触发、是否真正影响下游软件。

近年的研究正在推动依赖治理从“组件级发现”走向“影响范围精准判断”。USENIX Security 2025 的 ChainFuzz 关注上游漏洞在下游软件中的实际影响，提出通过自动生成 PoC 来验证上游漏洞是否能够在下游软件中被触发；其核心思想是，不能只因为一个下游项目依赖了某个脆弱组件，就直接认为它一定受影响，而要进一步验证漏洞在下游代码、约束和调用路径下是否真实可利用。 NDSS 2026 的 *From Noise to Signal* 则在 npm 生态中进行函数级漏洞影响测量，覆盖 3400 万个包版本，结果显示包级分析识别出的潜在影响中有 68\.28% 实际上是噪声，因为漏洞代码不可达；这说明传统依赖扫描容易产生大量误报，后续治理需要结合函数级可达性和依赖传播深度进行更精准的风险判断。

因此，依赖可视化与漏洞影响治理应分为三个层次。第一层是组件可见，即通过 SBOM 建立直接依赖和传递依赖清单，明确每个制品、镜像和系统中包含哪些组件。第二层是漏洞关联，即通过 SCA 将组件与 CVE、GHSA、OSV 等漏洞情报关联，发现潜在风险。第三层是影响判断，即结合调用路径、函数可达性、运行环境和业务暴露面，判断漏洞是否真正影响当前项目，并据此确定修复优先级。这样可以避免安全团队被大量低价值告警淹没，把治理资源集中在真正影响业务的供应链风险上。



## 代码贡献与仓库协作治理

在外部依赖完成准入并纳入持续治理之后，软件供应链还需要关注项目自身源码和配置的进入过程。代码贡献与仓库协作治理解决的是源码、配置、依赖声明和构建脚本如何可信地进入代码仓库的问题。这一环节是软件自身供应链的入口，攻击者可以通过恶意 Pull Request、维护者账号接管、敏感信息泄露、分支保护缺失或 CI/CD 配置篡改等方式污染源码基础。因此，这一阶段的核心目标是保证每一次进入主分支的变更都有可信身份、经过审查、权限受控并且可追溯，为后续自动化构建提供可信输入。

围绕这一目标，代码贡献与仓库协作治理需要重点解决三个层面的问题：第一是变更内容是否可信，第二是贡献者和维护者身份是否可信，第三是仓库协作流程是否可信。首先，在变更内容层面，攻击者可以伪装成普通贡献者提交恶意代码。恶意逻辑不一定以明显后门的形式出现，也可能隐藏在 bug 修复、性能优化、代码重构、依赖更新或构建脚本修改中。其次，在身份层面，攻击者可以通过维护者账号接管、社会工程或长期贡献建立信任后获得更高权限，从而绕过正常审查流程。最后，在协作流程层面，攻击者可以利用版本控制系统或仓库配置弱点，篡改源码、版本标签、Release 元数据或 CI/CD 配置。由于源码会进入后续构建和分发流程，一旦恶意代码被合并，下游用户无论是从源码构建还是安装预构建包，都可能受到影响。

在上述三个问题中，身份可信和贡献归因尤其容易被忽视。开源项目往往依赖贡献记录、账号活跃度和社区声誉来判断贡献者是否可信，但这些信任信号本身也可能被攻击者操纵。NDSS 2025 的论文 *Attributing Open\-Source Contributions is Critical but Difficult* 指出，开源贡献归因对软件供应链安全至关重要，但在 GitHub 实践中并不简单。该研究展示了攻击者可以利用 GitHub 贡献工作流操纵项目和个人资料，使自己看起来更加可信；论文对 50,328 个关键开源项目进行测量，发现 85\.9% 的项目贡献工作流存在可被滥用的空间。这个结论说明，代码贡献治理不能只看“某个账号是否有贡献记录”或“某个用户是否看起来活跃”，而应建立可验证的贡献归因机制，例如要求关键提交使用 commit signing，重要 release 使用 tag signing，对关键变更保留审查记录，并将贡献者身份、审批者身份和合并行为纳入审计链条。

因此，代码贡献治理首先需要建立严格的变更审查机制。所有进入主分支、发布分支和关键目录的修改都应通过 Pull Request 或 Merge Request 完成，禁止直接向生产分支推送代码。对于核心代码、依赖声明、构建脚本、CI/CD workflow、发布脚本和权限配置等高风险文件，应设置更高审查要求，例如强制至少两名维护者 Review、使用 CODEOWNERS 指定责任人、对敏感目录触发安全团队复核。这样做的目的，是防止攻击者通过看似普通的代码变更影响后续构建和发布过程。

其次，需要保护贡献者和维护者身份可信。维护者通常拥有代码合并、分支管理、Release 创建甚至包发布权限，是攻击者重点攻击的高价值目标。治理上应要求维护者和管理员账号启用多因素认证，避免密码复用，定期清理不活跃成员和长期有效的访问 Token，并遵循最小权限原则。对于外部贡献者，应限制其对仓库、构建 secrets 和发布流程的访问能力。贡献者身份和提交来源也应尽可能可验证，例如使用 commit signing、tag signing 或经过平台验证的身份机制，增强“谁提交了代码、谁批准了变更、哪个版本被发布”的可追溯性。

再次，需要强化版本控制系统自身的安全配置。版本控制系统不仅保存业务代码，也保存构建配置、依赖文件、发布标签、Release 元数据和协作记录，是软件供应链的源码根节点。仓库应启用分支保护，禁止强制推送和未审查合并；发布分支和版本标签应防止被覆盖、删除或伪造；Release 权限应与普通代码提交权限分离；仓库审计日志应记录关键操作，例如权限变更、分支保护规则变更、Release 创建和删除、密钥添加和删除等。

此外，源码仓库治理还必须关注敏感信息泄露问题。USENIX Security 2023 的 *Pushed by Accident* 研究表明，开发者在使用 GitHub、GitLab 等版本控制平台协作开发时，可能会将 API key、密码等代码密钥误提交到公共仓库中；论文通过问卷和访谈研究开发者处理源码密钥泄露的经验与策略，说明密钥泄露并不是偶发的低级错误，而是源码仓库协作中普遍存在的安全治理问题。 一旦这些凭证泄露，攻击者可能进一步访问构建系统、制品仓库、云资源或生产环境。因此，仓库应启用 secret scanning，在代码提交、Pull Request 和历史扫描中识别敏感凭证；对于已泄露凭证，应立即撤销、轮换并追踪使用痕迹。敏感信息治理的重点不是简单删除提交记录，而是阻断凭证被继续利用。



## CI/CD 流水线安全治理

当源码进入仓库后，CI/CD 流水线负责自动执行测试、构建、打包和发布任务，因此它解决的是**自动化执行过程是否会被攻击者滥用**的问题。CI/CD 不只是提升效率的工程工具，也掌握着构建环境、访问令牌、发布凭证和制品上传权限。攻击者即使不直接修改业务代码，也可能通过 workflow 注入、第三方 Action 污染、外部 PR 访问 secrets、runner 共享污染或任务权限过大来影响最终制品。因此，这一阶段的治理重点是保护流水线自身，确保自动化任务在受控权限、受控环境和受审查配置下执行。

围绕这一目标，CI/CD 流水线安全治理可以拆解为三个层面。第一是任务触发与脚本执行是否安全，即构建任务是否会执行来自 Pull Request、commit message、branch name、issue 内容等攻击者可控输入。第二是权限与凭证是否受控，即外部贡献者触发的任务是否能够访问 secrets，普通测试任务是否拥有发布权限，构建 Token 是否权限过大。第三是构建依赖与运行环境是否可信，即第三方 Action、CI 插件、runner、缓存目录和构建工作空间是否可能被污染。

近年来的研究也从不同角度印证了这些风险。IEEE S\&P 2023 的 *Continuous Intrusion: Characterizing the Security of Continuous Integration Services* 从 CI 服务的参与者、组件和工作流出发，系统分析了主流 CI 平台中的安全威胁，指出 CI/CD 服务可能引发任务劫持、仓库权限提升和制品劫持等问题。

在 GitHub Actions 场景下，USENIX Security 2023 的 ARGUS 论文进一步说明，workflow 文件本身也可能成为代码注入入口。攻击者可以通过 Pull Request 标题、commit message、branch name、issue 内容或环境变量等不可信上下文影响 workflow 中的 shell 命令或脚本参数，从而触发命令注入、凭证泄露或恶意制品发布。

NDSS 2026 的 *Action Required: A Mixed\-Methods Study of Security Practices in GitHub Actions* 则从真实采用情况角度补充了治理问题。该研究发现，GitHub Actions 中多类关键安全实践采用率并不高，主要障碍包括开发者缺乏认知、误解适用场景以及担心运维成本。这说明 CI/CD 安全治理不能只依赖开发者自发配置，而需要组织层面的制度化要求，尤其是在权限最小化、secrets 隔离、第三方 Action 固定版本和 workflow 审查等方面形成统一规范。

基于上述问题，CI/CD 流水线安全治理可以从以下方面展开。首先，应将 workflow 和构建脚本纳入代码审查范围。CI/CD 配置文件决定了构建任务如何触发、执行哪些命令、访问哪些凭证、上传哪些制品，因此其安全影响不亚于业务代码。对于修改 GitHub Actions workflow、Jenkinsfile、Dockerfile、发布脚本和依赖安装脚本的 Pull Request，应设置更严格的审查规则，必要时由安全团队或平台团队参与 Review。同时，可以借鉴 ARGUS 所代表的静态污点分析思路，识别 workflow 中不可信输入到危险命令的传播路径，例如命令注入、脚本拼接、未转义变量和危险 shell 执行等问题。

其次，应实施权限最小化和任务隔离。CI/CD 流水线通常持有代码仓库访问权限、制品仓库上传权限、云服务凭证、包发布 Token 和部署密钥。如果普通测试任务、外部贡献者触发的任务和正式发布任务共享同一权限集合，一旦低权限任务被注入，攻击者可能进一步获取高价值凭证。因此，应区分测试构建、内部构建和发布构建；外部 Pull Request 触发的 workflow 不应访问生产 secrets；普通构建任务不应拥有发布权限；发布 Token 应使用短生命周期、最小作用域和按需授权；制品上传、镜像推送和生产部署应放在独立、受保护的 release workflow 中。

此外，应加强第三方 Action、插件和构建依赖的供应链管理。GitHub Actions、Jenkins 插件和 CI 模板本身也是供应链依赖。如果 workflow 直接使用第三方 Action 的浮动版本，上游 Action 一旦被篡改，下游构建流程就会受到影响。因此，应固定第三方 Action 到明确版本或 commit hash，避免使用未审查的第三方 Action；对高权限 Action 建立准入审批；定期检查 Action 和插件的维护状态、漏洞信息和权限需求。对于自托管 runner，还应防止不同项目或不同任务之间共享污染，例如缓存目录、工作空间、Docker socket、构建制品目录和临时文件未隔离等问题。



## 构建可信与来源证明治理

在 CI/CD 流水线安全执行的基础上，构建可信与来源证明治理进一步解决的是**最终制品是否真的按照规定流程生成**的问题。也就是说，不能只相信流水线显示“构建成功”，还要能够证明这个制品来自哪个源码版本、使用了哪些依赖、经过了哪些测试和扫描、由哪些主体执行，以及步骤之间的产物是否被替换。以 in\-toto、SLSA、provenance 和 attestation 为代表的治理思路，就是将构建过程从黑盒执行转变为可验证证据链，使下游用户或部署系统能够验证制品的来源和生成过程。

围绕这一目标，构建可信治理需要重点解决三类问题。第一是来源一致性，即最终软件包是否真的由对应源码、依赖和构建配置生成；第二是步骤完整性，即构建过程中规定的测试、扫描、打包、审计等步骤是否都被执行，是否存在跳过或替换；第三是产物流完整性，即源码、中间产物和最终制品在步骤之间传递时是否发生未记录、未授权的修改。这些问题的共同点在于，它们都不是单靠“构建成功”日志就能证明的，而需要对每个关键步骤产生可验证的元数据和证据。

USENIX Security 2019 的 in\-toto 论文正是针对这一问题提出了端到端的软件供应链完整性保障框架。论文指出，现代软件开发过程包含源码提交、测试、构建、打包、分发等多个步骤，攻击者只要控制其中任意一个步骤，就可能修改软件并影响最终用户。它将软件供应链作为一个整体进行建模和验证，通过密码学方式记录并验证每一个关键步骤。**in\-toto 的基本思路可以概括为：项目所有者先定义“软件供应链应该如何执行”，每个执行者再证明“自己确实按照要求执行了对应步骤”，最终由下游用户或部署系统验证“整个供应链过程是否符合预期”。**其中，项目所有者预先声明并签署一份供应链 layout。layout 可以理解为软件供应链的“流程蓝图”，用于描述构建过程包括哪些步骤、每个步骤由谁执行、每个步骤允许使用哪些输入材料、允许产生哪些输出产物，以及不同步骤之间的产物流动关系。

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=OTU2NDk4YTJiNGFlYWI3MDA2ZTFiNTA3YmY5NmU2YTRfZjZiNDdhODhjYmZkODkwZTBlODMxYmRkYmU2ODk2MzNfSUQ6NzY0NzQ0NzQ0NjMwMjI2NDI3OV8xNzgwNzUwODQwOjE3ODA4MzcyNDBfVjM)

当每个步骤实际执行时，执行者，即 in\-toto 中的 functionary，需要生成并签署 link metadata。link metadata 记录该步骤使用了哪些输入材料、生成了哪些输出产物、执行了什么命令、执行结果如何等信息。其中，输入材料被称为 materials，输出产物被称为 products，二者通常以文件路径和密码学哈希的形式记录。这样，下游验证者就可以检查上一步产生的 products 是否与下一步使用的 materials 完全一致。如果攻击者在两个步骤之间替换了文件，那么前一步记录的输出哈希与后一步记录的输入哈希就无法匹配，验证过程会失败。in\-toto 还要求 link metadata 由对应 functionary 的私钥签名，从而证明该步骤确实由 layout 中授权的主体执行。

从治理角度看，in\-toto 提供了三类关键能力。第一是供应链步骤完整性，即验证所有规定步骤是否都被执行，是否存在步骤缺失、跳过或重排。第二是产物流完整性，即验证上一步产生的产物是否被下一步正确使用，中间是否发生未授权替换。第三是步骤执行者认证，即验证每个步骤是否由 layout 中指定的主体执行，而不是由攻击者或未授权系统伪造执行记录。论文在安全分析中也指出，在攻击者只能控制基础设施或通信链路、但无法获得 functionary 签名密钥的情况下，攻击者无法在两个连续步骤之间替换产物，因为 products 和 materials 的哈希将无法匹配；也无法提供缺失步骤或重排步骤的制品，因为对应 link metadata 会缺失或顺序不符合 layout；同时也无法伪造未授权步骤的 link metadata。

因此，in\-toto 所代表的治理思路并不是简单地“加一个构建日志”或“记录一次 CI/CD 执行结果”，而是要求构建过程中的每个关键步骤都形成可验证证明，并将这些证明与项目所有者定义的供应链 layout 进行匹配。它把构建可信从“相信构建系统”转变为“验证构建证据”。在实际治理中，组织可以要求关键项目定义标准化构建流程，对源码获取、依赖解析、测试、安全扫描、构建、打包和发布等步骤生成 provenance 或 attestation，并在制品进入仓库或部署环境前进行强制验证。这样，最终制品不仅有构建结果，还有能够说明其来源、路径和执行主体的完整证据链。



## 制品签名与发布治理

在完成构建可信与来源证明之后，软件供应链治理还需要进一步保护最终制品在发布和分发阶段的可信性。也就是说当该制品被上传到制品仓库、包管理平台、容器镜像仓库、CDN 或 GitHub Releases 后，仍然可能在发布、托管、分发和下载过程中被替换或篡改。因此，供应链治理不能只停留在“构建过程是否可信”，还必须回答一个更靠近下游用户的问题：用户最终拿到的软件制品是否真的来自可信发布者，并且在发布后没有被替换。

制品签名与发布治理正是为了解决这一问题。它需要建立一套能够绑定“发布身份、软件制品和发布行为”的可信机制。具体来说，它至少要解决三个核心问题：第一，身份问题，即谁签署并发布了这个软件制品；第二，完整性问题，即软件包、容器镜像或二进制文件在发布和传输过程中是否被篡改；第三，可追溯问题，即签名和发布行为是否能够被公开验证和事后审计。

**Sigstore 正是针对这些问题提出的一套面向大规模软件生态的软件签名基础设施。**

Sigstore 的目标是提供一种“人人可用”的软件签名机制。它并不是重新发明数字签名，而是试图解决传统软件签名难以大规模落地的问题。传统签名体系通常要求开发者生成长期密钥、保存私钥、发布公钥、管理证书、处理密钥轮换和撤销。这对于普通开源维护者来说非常复杂，也容易出错。一旦长期私钥泄露，攻击者可能长期伪造签名，影响历史和未来版本。

Sigstore 的思路是：**把传统软件签名中的“长期密钥管理问题”，转化为“基于现有身份的短期签名问题”。**

具体来说，它有三个关键设计：

第一，使用 **OIDC 身份认证**。开发者不需要自己建立新的身份体系，而是可以使用已有身份提供方，例如 GitHub、Google、Microsoft 等，通过 OpenID Connect 证明自己控制某个身份。

第二，使用 **短期证书和临时密钥**。开发者在签名时生成一次性密钥对，通过 OIDC 身份向 Sigstore 的 CA 申请短生命周期证书，用该临时私钥签名制品，然后丢弃私钥。论文中提到，Sigstore 使用短生命周期证书，默认有效期为 10 分钟，签名完成后私钥材料会被丢弃，从而降低长期密钥泄露风险。

第三，使用 **透明日志**。Sigstore 不仅生成签名，还将证书和制品签名写入公开透明日志，使任何人都可以检查签名行为是否发生、是否异常、是否与某个身份相关。这种机制借鉴了 Web PKI 中证书透明性的思想。

![Image](https://internal-api-drive-stream.feishu.cn/space/api/box/stream/download/authcode/?code=OTRiMjU3Zjg4MmI4MjFjY2Y0Zjc3ZDI5ZmZhYmYzMWFfZDU5NDhjMWY5YTE1OWYyYmFkZGQ2NjBjNDNlYjhkNDlfSUQ6NzY0NzQ0MTE0NjAwMDEzMzEwM18xNzgwNzUwODQwOjE3ODA4MzcyNDBfVjM)

Sigstore 主要由三个核心组件构成：**Cosign、Fulcio 和 Rekor**，并结合 OIDC 身份提供方、TUF 根信任机制和软件制品仓库共同完成签名与验证流程。

- Cosign：客户端签名与验证工具：Cosign 是 Sigstore 的参考客户端实现。开发者使用 Cosign 对软件制品进行签名，例如容器镜像、二进制文件、软件包、SBOM 或 provenance 文件。验证者也可以使用 Cosign 检查制品签名是否有效。

- Fulcio：证书颁发机构与身份日志：Fulcio 是 Sigstore 中的证书颁发机构，也承担身份日志相关功能。它负责验证开发者的 OIDC 身份令牌，并为开发者生成的临时公钥签发短生命周期证书。

    1. 开发者通过 OIDC 身份提供方证明自己控制某个身份，例如 GitHub 账号或邮箱身份；

    2. Cosign 在本地生成一次性密钥对；

    3. Cosign 向 Fulcio 提交 OIDC token、公钥和证明；

    4. Fulcio 验证 OIDC token 是否有效；

    5. Fulcio 为该公钥签发短期证书，并将证书与身份绑定。

- Rekor：制品签名透明日志：Rekor 是 Sigstore 的制品签名透明日志，也就是 Artifact Log。它负责记录制品签名、证书和相关元数据，使签名行为变成公开、可审计的记录。

    - 透明日志的作用不是替代签名，而是增强签名的可追溯性。没有透明日志时，攻击者如果短暂控制发布账号或签名密钥，可能悄悄发布恶意版本。引入透明日志后，签名和证书都会被记录到公开日志中，后续可以由维护者、平台、用户或监控系统检查是否存在异常身份、异常签名或异常发布。

- TUF：根信任与密钥更新机制：Sigstore 还使用 The Update Framework，也就是 TUF，来管理根信任。TUF 的作用是帮助客户端安全获取和更新 Sigstore 生态中的根证书、Fulcio 和 Rekor 的信任材料，并防止 freeze attack、replay attack 等攻击。

从治理角度看，Sigstore 解决的是发布阶段的“最后一公里可信”问题。它的价值在于证明某个制品确实由某个可信身份签署，并且签名行为被记录在公开透明日志中。这样，即使软件仓库或分发平台被攻击者控制，只要攻击者无法获得可信身份对应的签名能力，伪造或替换的软件包就无法通过验证。即使某个身份或签名流程出现异常，透明日志也能为事后审计和异常检测提供依据。

---

# 六、开源软件供应链安全治理面临的挑战

虽然开源组件准入、依赖扫描、SBOM、CI/CD 安全控制、构建来源证明和制品签名等措施能够提升软件供应链安全水平，但在实际落地中仍然存在一些困难。

1. 供应链依赖关系复杂，完整可见性难以建立。

    一个软件项目不仅包含直接依赖，还可能包含大量传递依赖、容器基础镜像、系统软件包和构建阶段引入的临时组件。即使生成 SBOM，也需要保证其随版本持续更新，并能与漏洞情报和资产系统联动，否则很难真正用于影响范围分析。

2. 漏洞告警和真实风险之间存在差距。

    SCA 工具通常能够发现某个组件版本存在漏洞，但并不能总是判断该漏洞在当前项目中是否可达、是否会被触发、是否暴露在真实业务场景中。因此，供应链漏洞治理容易出现大量告警，后续仍需要结合调用路径、运行环境和业务暴露面进行优先级判断。

3. 可信构建和制品验证的落地成本较高。

    SLSA、in\-toto、Sigstore 等机制可以提升构建过程和发布制品的可信度，但它们需要改造现有 CI/CD 流程、管理签名策略、保存来源证明，并在制品入库或部署阶段进行强制验证。对于小型项目或维护资源有限的开源项目而言，完整落地并不容易。

4. 软件供应链涉及多个主体，责任边界较为复杂。

    贡献者、维护者、包管理平台、CI/CD 服务商、分发平台和下游用户都可能影响供应链安全。安全事件发生后，责任往往难以简单划分。因此，软件供应链治理不仅需要技术工具，也需要权限管理、流程规范、平台责任和下游用户自身安全管理共同配合。

总体来看，软件供应链安全治理不是部署某个工具就能完成的工作，而是需要在软件开发、构建、分发和使用全过程中持续建立可见性、可信度和责任追溯能力。

# 七、新场景下的软件供应链安全治理

## 开发者工具链与 IDE 插件供应链风险

开发者工具链正在成为软件供应链安全治理中一个容易被忽视的新入口。传统供应链治理更多关注开源依赖、代码仓库、CI/CD 流水线和制品发布，但在实际开发过程中，代码进入仓库之前通常已经经过 IDE、编辑器插件、格式化工具、调试工具、代码补全工具和远程开发环境的处理。这些工具运行在开发者本机或云端开发环境中，往往能够访问源码、项目配置、环境变量、Git 凭证、API Token 和构建脚本。因此，如果开发者工具或 IDE 插件存在漏洞、权限过大，或者被攻击者控制，攻击就可能发生在代码进入仓库之前，进而影响后续构建、发布和部署流程。

NDSS 2024 的 *UntrustIDE: Exploiting Weaknesses in VS Code Extensions* 关注的正是这一问题。该论文指出，随着软件供应链攻击增多，开发者 IDE 成为攻击者有吸引力的目标，并系统分析了 VS Code 扩展生态中的安全弱点。该研究说明，IDE 插件不应被视为供应链之外的个人效率工具，而应被纳入开发阶段的软件供应链治理范围。

因此，开发者工具链治理需要从“默认信任插件”转向“插件准入与权限控制”。组织可以建立 IDE 插件和开发工具白名单机制，对高权限插件、代码补全工具、远程开发插件和 AI 编程助手进行安全评估；限制插件对源码、凭证、环境变量和构建配置的访问能力；对插件版本进行固定和更新审查，避免自动引入不可信版本；在企业场景下，还可以使用受控开发镜像、远程开发容器或统一插件市场，减少开发者随意安装来源不明插件的风险。这个场景说明，软件供应链治理的边界正在前移到“代码产生和修改的开发环境”。

## AI 辅助开发带来的新型供应链风险

在开发者工具链风险之外，AI 辅助开发进一步改变了代码和依赖的生成方式。开发者不再只是手工编写代码，而是越来越多地使用大语言模型生成函数实现、依赖声明、安装命令、配置文件，甚至 CI/CD workflow。与 IDE 插件风险不同，AI 辅助开发的核心问题不只是工具本身是否安全，而是 AI 生成的内容是否可信。模型可能生成不存在的包名、过时依赖、不安全配置或来源不明的代码片段。如果开发者直接复制并执行这些内容，就可能在不知情的情况下将恶意依赖或不安全配置引入项目。

其中，package hallucination 是一个典型问题。USENIX Security 2025 的 *We Have a Package for You\!* 系统研究了代码生成模型中的包名幻觉现象。论文指出，LLM 可能在生成代码时推荐并不存在的软件包，这类错误会形成新的 package confusion 攻击面：攻击者可以提前注册这些被模型“幻觉”出的包名，并植入恶意代码，等待开发者根据 AI 建议安装。该研究使用 16 个代码生成模型，生成并分析了 576,000 个 Python 和 JavaScript 样本，说明这一问题并非个别模型偶发错误，而是 AI 辅助开发场景下值得关注的新型供应链风险。

因此，AI 辅助开发场景下的治理重点，是把 AI 生成内容视为不可信输入。组织应对 AI 生成的依赖声明进行真实性校验，不允许未经验证的软件包直接进入项目；将 AI 生成代码纳入常规代码审查、SCA 扫描和安全测试；对 AI 生成的安装命令、Dockerfile、workflow 和配置文件进行安全审查；在开发工具中加入依赖真实性检查，提醒开发者某个包是否真实存在、是否来自可信仓库、是否存在名称混淆风险。

## 智能汽车与 OTA 场景下的软件供应链安全治理

随着汽车从传统机械产品转向软件定义汽车，车辆中的软件规模和更新频率不断增加。现代汽车软件不仅包括车机系统和联网服务，也涉及 ECU、域控制器、辅助驾驶、电池管理、车联网通信等多个安全关键模块。与此同时，汽车软件通常由芯片供应商、零部件供应商、主机厂、云端 OTA 平台和车端系统共同参与交付，形成了比普通开源软件更复杂的多方供应链。

*NDSS 2026 Securing Automotive Software Supply Chains* 关注的正是这一场景下的软件供应链安全问题。该论文指出，在汽车软件供应链中，软件更新不仅需要保证更新包本身的完整性和来源可信，还需要考虑供应链元数据如何被验证、验证责任应放在哪些节点，以及车端是否适合承担完整验证。论文的一个核心观点是，在车辆端完整验证所有软件供应链元数据不仅效率较低，而且在存在多个仓库侧验证点的情况下，对安全性来说并不总是必要的。因此，更合理的治理思路是在仓库侧、OTA 平台侧和供应链中间节点设置多个验证点，对供应链元数据进行分层验证，再由车辆端执行必要的轻量级最终校验。

这一研究说明，软件供应链治理在智能汽车场景下面临新的约束。首先，汽车软件供应链涉及多级供应商和多个责任主体，必须解决供应商软件来源、版本、签名、依赖和更新记录的可追溯问题。其次，汽车软件更新具有安全关键属性，错误更新或恶意更新可能影响车辆运行安全，因此治理目标不仅是防篡改，还包括更新失败处理、版本回滚、责任追踪和合规审计。再次，车端设备存在算力、存储、网络和实时性限制，不能简单照搬通用软件供应链中“客户端完整验证所有元数据”的模式，而需要结合 OTA 架构进行分层验证和轻量化设计。

因此，汽车软件供应链治理可以从三个方向展开：第一，在供应商、主机厂和 OTA 平台之间建立软件物料清单、制品签名和来源证明机制，确保每个软件模块都能追溯到来源、版本和构建过程；第二，在仓库侧和 OTA 平台侧部署更完整的供应链元数据验证，提前过滤不满足策略的软件包；第三，在车端执行轻量级签名、版本和策略校验，保证最终安装的软件包来自可信更新通道，并支持安全回滚和审计。这个场景说明，未来软件供应链治理不能只关注通用开源生态，还需要根据汽车、工业控制、医疗设备等安全关键行业的资源约束和物理安全要求进行场景化设计。

## 自动化补丁与 AI 驱动治理方向

随着软件供应链规模扩大，依赖漏洞和安全告警数量快速增长，传统依赖人工分析和修复的方式已难以支撑大规模响应。现有 SCA 工具通常能够发现“某个组件版本存在漏洞”，但很难进一步判断该漏洞是否位于实际调用路径、是否暴露在业务场景中、是否具有真实修复优先级。因此，AI 驱动治理的价值不应仅理解为“自动生成补丁”，而应体现在漏洞影响判断、候选修复生成和补丁验证的闭环中。

在漏洞响应过程中，AI 可以结合 SBOM、漏洞公告、依赖关系、调用路径和代码上下文，辅助判断某个漏洞是否真正影响当前系统。例如，项目虽然依赖了存在漏洞的组件，但如果漏洞函数不可达，或调用路径不经过外部可控输入，其修复优先级就应低于真实可达且暴露面较大的漏洞。在确认风险真实存在后，AI 可以进一步根据上游补丁、项目代码和版本约束生成候选修复方案，包括依赖升级、API 替换、配置加固或代码修改。不过，AI 生成结果只能作为候选补丁，而不能直接视为可信修复；模型可能生成语义不完整的修改，也可能只规避触发条件而没有消除漏洞根因，甚至引入新的兼容性或安全问题。

因此，AI 生成补丁必须进入标准化验证流程。补丁不仅需要通过编译和单元测试，还应结合回归测试、安全测试、静态分析、依赖扫描，必要时还可通过 AI 辅助生成针对漏洞触发路径的测试用例。经过验证的补丁仍不应绕过供应链治理流程，而应通过代码审查、受控 CI/CD、构建来源证明、制品签名和灰度发布进入生产环境。也就是说，AI 在这一场景中的角色是提升漏洞分析和修复效率，而不是替代安全决策。

这一方向也与自动化漏洞修复研究相呼应。USENIX Security 2025 的 *SoK: Automated Vulnerability Repair* 将自动化漏洞修复概括为漏洞分析、补丁生成和补丁验证三个阶段，说明自动化补丁治理不是简单生成代码，而是需要完整验证闭环。APPATCH、VRpilot 等工作进一步探索了利用大语言模型生成真实软件漏洞补丁，但这类方法的安全落地仍依赖编译、测试、sanitizer 或人工审查反馈。由此可见，未来软件供应链中的 AI 驱动治理，应形成“影响分析—候选修复—验证审查—可信发布”的流程。

---

# 八、总结

软件供应链安全治理是互联网治理中非常重要但容易被忽视的方向。现代互联网服务依赖大量开源组件、第三方SDK、云服务、外包代码和自动化构建工具，这些依赖关系提高了开发效率，也带来了新的安全风险。

与传统漏洞管理不同，软件供应链安全治理关注的不是某一个孤立漏洞，而是软件从开发、构建、交付、部署到更新全过程中的风险控制。它要求组织知道软件从哪里来、由什么组成、经过什么流程、由谁负责、出事后如何响应。

因此，软件供应链安全治理的核心可以概括为四句话：

第一，来源可见。组织要知道软件中包含哪些组件和依赖。

第二，过程可信。软件开发、构建、发布和更新流程要受到保护和审计。

第三，责任可追。供应商、开发者、平台和组织内部部门的责任要明确。

第四，风险可控。安全事件发生后，能够快速定位、快速修复、快速复盘。

从更大的互联网治理角度看，软件供应链安全不仅是企业内部安全问题，也是互联网生态安全问题。一个组件、一个供应商、一个更新机制的失控，都可能影响大量下游系统和用户。未来的网络空间安全治理，不能只关注单个系统是否安全，还必须关注支撑整个互联网服务的软件供应链是否安全、可信和可管理。

因此，软件供应链安全治理体现了网络空间安全从“单点防护”走向“生态治理”的趋势，也为互联网治理提供了一个兼具技术基础和管理意义的重要研究方向。

---

# 附

